#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
    int n, count;
    cin >> n;
    vector<vector<int> > a(n, vector<int>(n));

    for(int i=0; i<n; i++){
        for(int j=0; j<n; j++){
            cin >> a[i][j];
        }
    }

    vector<int> current(n+1);
    for(int i=1; i<n+1; i++){

    }


    return 0;
}